# GWSat v3 — Requirements
# Python 3.10+ recommended

# Core
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
# TerraMind (IBM foundation model — REQUIRED for hackathon)
# Primary: official terratorch package (install from GitHub)
git+https://github.com/IBM/terratorch.git

# Fallback A: HuggingFace transformers (if terratorch unavailable)
transformers>=4.35.0
huggingface_hub>=0.19.0

# Fallback B: timm proxy (DeiT-tiny)
timm>=0.9.0

# Data ingestion
rasterio>=1.3.0
pandas>=2.0.0
Pillow>=10.0.0

# Training / evaluation
scikit-learn>=1.3.0
tqdm>=4.65.0

# Demo
gradio>=4.0.0
matplotlib>=3.7.0

# GEE pipeline (optional)
earthengine-api>=0.1.380

# Export / deployment
onnx>=1.15.0
onnxruntime>=1.15.0